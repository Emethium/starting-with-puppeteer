const { measureTimeElapsed } = require("./time");
const { takeSnapshot, takeScreenshot, takeContent } = require("./page");

module.exports = {
  measureTimeElapsed,
  takeSnapshot,
  takeScreenshot,
  takeContent
};
